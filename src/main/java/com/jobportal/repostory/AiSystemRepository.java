package com.jobportal.repostory;

import com.jobportal.entity.AiSystem;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AiSystemRepository extends JpaRepository<AiSystem, Integer> {
}
